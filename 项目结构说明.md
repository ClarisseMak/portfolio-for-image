# 📚 项目结构说明

## 🎯 页面结构

### 首页 (/)
- **个人简介**：展示照片和关于我
- **项目列表**：按分类展示所有项目
  - 每个项目卡片显示：标题图、标题、描述
  - 点击卡片进入详情页
- **特别感谢**：展示感谢的人员或组织
  - 显示头像、姓名
  - 可点击跳转到个人主页

### 项目详情页 (/project/:id)
- **返回按钮**：回到首页
- **项目头部**：分类标签、标题、描述、标题图
- **项目详情**：展示 main 中的所有图文内容

## 📂 文件结构

```
src/
├── assets/
│   ├── profile.json              # 个人信息
│   ├── thanks.json               # 特别感谢数据
│   └── projects/
│       └── example.json          # 项目数据
├── components/
│   ├── AboutMe.vue               # 个人简介组件
│   ├── ProjectCard.vue           # 首页项目卡片（新）
│   ├── ProjectItem.vue           # 详情页项目内容
│   └── SpecialThanks.vue         # 特别感谢组件
├── views/
│   ├── Home.vue                  # 首页（新）
│   └── ProjectDetail.vue         # 项目详情页（新）
├── router/
│   └── index.js                  # 路由配置（新）
├── App.vue                       # 主应用
└── main.js                       # 入口文件
```

## 📊 数据结构

### profile.json

```json
{
  "name": "姓名",
  "nameEn": "英文名",
  "major": "专业",
  "school": "学校",
  "photo": "照片URL",
  "aboutMe": "关于我的介绍",
  "social": [
    {
      "name": "平台名称",
      "icon": "图标",
      "url": "链接"
    }
  ]
}
```

### example.json

```json
{
  "projects": [
    {
      "id": 0,
      "category": "分类名称",        // 用于首页分类展示
      "title": "项目标题",
      "titleImage": "标题图URL",     // 首页和详情页展示
      "description": "项目描述",     // 首页和详情页展示
      "main": [                      // 只在详情页展示
        {
          "title": "内容标题",
          "description": "内容描述",
          "image": "图片URL",
          "link": "相关链接（可选）"
        }
      ]
    }
  ]
}
```

### thanks.json

```json
{
  "thanks": [
    {
      "avatar": "头像URL",           // 可选，头像图片
      "name": "姓名或昵称",
      "url": "个人主页链接"           // 可选，点击头像/名字跳转
    }
  ]
}
```

## 🎨 组件说明

### 1. AboutMe.vue
**用途**：展示个人简介  
**位置**：首页顶部  
**展示内容**：
- 个人照片（左侧）
- 姓名、学校、专业
- 关于我的介绍
- 社交媒体链接

### 2. ProjectCard.vue (新)
**用途**：首页项目卡片  
**位置**：首页项目区域  
**展示内容**：
- 标题图片（titleImage）
- 项目标题（title）
- 项目描述（description）
- 悬停效果和查看按钮

**交互**：点击卡片跳转到详情页

### 3. ProjectItem.vue
**用途**：详情页展示完整内容  
**位置**：项目详情页  
**展示内容**：
- main 数组中的所有图文内容
- 左右交替的图文布局
- 图片灯箱预览功能

### 4. Home.vue (新)
**用途**：首页容器  
**功能**：
- 组合 AboutMe 和项目列表
- 按 category 分组展示项目
- 使用 ProjectCard 组件

### 5. ProjectDetail.vue (新)
**用途**：项目详情页  
**功能**：
- 显示返回按钮
- 展示项目完整信息
- 使用 ProjectItem 展示详细内容
- 404 处理

### 6. SpecialThanks.vue
**用途**：特别感谢组件  
**位置**：首页底部  
**展示内容**：
- 感谢对象的头像（如果有）
- 姓名或昵称
- 可点击跳转到个人主页
- 优雅的卡片式布局
- 响应式网格排列

**交互**：点击卡片跳转到对应的个人主页或链接

## 🚀 路由配置

```javascript
/                    → Home 页面（个人简介 + 项目列表）
/project/:id         → ProjectDetail 页面（项目详情）
/*                   → 重定向到首页
```

## 💡 使用流程

### 添加新项目

1. 编辑 `src/assets/projects/example.json`
2. 在 projects 数组中添加新项目：

```json
{
  "id": 3,                                    // 唯一ID
  "category": "School Projects",              // 分类
  "title": "新项目标题",
  "titleImage": "标题图URL",
  "description": "项目简短描述",
  "main": [
    {
      "title": "细节1",
      "description": "详细说明",
      "image": "图片URL",
      "link": "相关链接"
    }
  ]
}
```

### 添加感谢对象

1. 编辑 `src/assets/thanks.json`
2. 在 thanks 数组中添加新对象：

```json
{
  "avatar": "https://example.com/avatar.jpg",  // 可选
  "name": "感谢对象的名字",
  "url": "https://example.com/profile"         // 可选
}
```

**注意**：
- `avatar` 和 `url` 都是可选字段
- 如果没有 `avatar`，会显示默认头像图标
- 如果没有 `url`，卡片将不可点击

### 修改分类

项目会自动按 `category` 字段分组，可以使用的分类示例：
- School Projects（学校项目）
- Company Projects（公司项目）
- Personal Projects（个人项目）
- Design Works（设计作品）
- Research Projects（研究项目）

### 图片处理

**首页显示**：
- titleImage：标题图，建议 16:9 比例

**详情页显示**：
- main 中的 image：详细图片，建议统一尺寸

**下载图片到本地**：
```bash
python download_images.py
```

## 🎯 页面特点

### 首页
- ✨ 简洁清晰的布局
- 📱 响应式网格（自适应列数）
- 🏷️ 按分类组织项目
- 🎴 悬停效果和动画
- 💝 特别感谢区域展示致谢对象

### 详情页
- ↩️ 便捷的返回按钮
- 🖼️ 大图展示
- 📋 完整的项目信息
- 🔍 图片灯箱功能

## 🔧 自定义样式

### 修改首页网格列数

在 `Home.vue` 中：
```css
.projects-grid {
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  /* 修改 320px 来调整卡片最小宽度 */
}
```

### 修改卡片高度

在 `ProjectCard.vue` 中：
```css
.card-image {
  aspect-ratio: 16 / 9;  /* 修改比例 */
}
```

### 修改主题色

全局搜索并替换：
- `#667eea` → 你的主色
- `#764ba2` → 你的次色

## 📱 响应式断点

- **桌面端** (>968px)：多列网格，左右布局
- **平板** (768px-968px)：2列网格
- **移动端** (<768px)：单列布局

## 🐛 常见问题

### Q: 图片加载不出来？
A: 运行 `python download_images.py` 下载到本地

### Q: 如何修改分类显示顺序？
A: 分类按首次出现顺序显示，调整 JSON 中项目顺序即可

### Q: 如何隐藏某个分类？
A: 暂时删除或注释该分类下的所有项目

### Q: 详情页返回如何保持滚动位置？
A: 已通过路由的 `scrollBehavior` 配置自动处理

### Q: 如何添加或删除感谢对象？
A: 编辑 `src/assets/thanks.json` 文件，在 thanks 数组中添加或删除对象

### Q: 感谢卡片没有头像怎么办？
A: 可以省略 `avatar` 字段，组件会显示默认的用户图标

## 🚀 部署建议

构建生产版本：
```bash
pnpm build
```

生成的 `dist` 文件夹可以部署到：
- Vercel
- Netlify  
- GitHub Pages
- 云服务器

**注意**：如使用 GitHub Pages，需要配置 `base` 路径。

---

如有问题，请查看各组件源码中的注释。

