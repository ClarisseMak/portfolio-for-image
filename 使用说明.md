# 服装设计作品集 - 使用说明

## 📸 项目简介

这是一个简洁优雅的个人作品集网站，专为艺术/设计类专业学生打造。

## 🎨 功能特点

- ✨ **个人简介**：展示个人照片和"关于我"
- 🖼️ **作品展示**：图文并茂的项目展示
- 💝 **特别感谢**：展示感谢的人员或组织
- 📱 **响应式设计**：完美适配桌面和移动设备
- 🎯 **简洁美观**：专注于作品本身

## 📝 如何使用

### 1. 修改个人信息

编辑文件：`src/assets/profile.json`

```json
{
  "name": "你的名字",
  "nameEn": "Your Name",
  "major": "服装设计",
  "school": "你的大学",
  "photo": "你的照片链接或路径",
  "aboutMe": "关于你的介绍...\n\n可以分多个段落",
  "social": [
    {
      "name": "Bilibili",
      "icon": "📺",
      "url": "你的B站链接"
    }
  ]
}
```

### 2. 添加个人照片

**方法一：使用本地照片（推荐）**

1. 将照片放到 `public/images/` 目录
2. 在 `profile.json` 中设置：
   ```json
   "photo": "/images/my-photo.jpg"
   ```

**方法二：使用在线图片**
```json
"photo": "https://example.com/your-photo.jpg"
```

💡 **照片建议**：
- 尺寸：800x1000px 左右（3:4 比例）
- 格式：JPG 或 PNG
- 大小：控制在 500KB 以内
- 风格：清晰、专业、符合个人风格

### 3. 编辑作品项目

编辑文件：`src/assets/projects/example.json`

```json
{
  "projects": [
    {
      "id": 0,
      "title": "项目系列标题",
      "main": [
        {
          "title": "作品名称",
          "description": "作品描述",
          "image": "作品图片链接",
          "link": "相关链接（可选）"
        }
      ]
    }
  ]
}
```

### 4. 添加感谢对象

编辑文件：`src/assets/thanks.json`

```json
{
  "thanks": [
    {
      "avatar": "头像图片URL",
      "name": "姓名或昵称",
      "url": "个人主页链接"
    }
  ]
}
```

**字段说明**：
- `avatar`（可选）：感谢对象的头像图片
- `name`（必填）：姓名或昵称
- `url`（可选）：个人主页、GitHub、Instagram 等链接

💡 **小贴士**：
- 如果没有 `avatar`，会显示默认的用户图标
- 如果没有 `url`，卡片将不可点击
- 可以添加多个感谢对象

**示例**：
```json
{
  "thanks": [
    {
      "avatar": "https://example.com/avatar.jpg",
      "name": "张老师",
      "url": "https://example.com/profile"
    },
    {
      "name": "开源社区",
      "url": "https://github.com"
    },
    {
      "avatar": "https://example.com/friend.jpg",
      "name": "好友小明"
    }
  ]
}
```

### 5. 添加作品图片

**推荐：下载到本地**

如果你的图片在B站等平台，使用提供的脚本下载：

```bash
# 安装 Python 依赖
pip install requests

# 运行下载脚本
python download_images.py
```

脚本会自动：
- 下载所有图片到 `public/images/projects/`
- 更新 JSON 文件为本地路径
- 备份原文件

### 6. 启动项目

```bash
# 安装依赖（首次运行）
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build
```

## 📂 文件结构

```
portfolio/
├── src/
│   ├── assets/
│   │   ├── profile.json          # 个人信息
│   │   ├── thanks.json           # 特别感谢数据
│   │   └── projects/
│   │       └── example.json      # 作品信息
│   ├── components/
│   │   ├── AboutMe.vue           # 个人简介组件
│   │   ├── ProjectCard.vue       # 项目卡片组件
│   │   ├── ProjectItem.vue       # 作品展示组件
│   │   └── SpecialThanks.vue     # 特别感谢组件
│   ├── views/
│   │   ├── Home.vue              # 首页
│   │   └── ProjectDetail.vue     # 项目详情页
│   ├── router/
│   │   └── index.js              # 路由配置
│   └── App.vue                   # 主应用
├── public/
│   └── images/                   # 存放图片
│       └── projects/             # 作品图片
└── download_images.py            # 图片下载脚本
```

## 🎨 自定义样式

### 修改配色

在 `src/components/AboutMe.vue` 中查找并修改渐变色：

```css
/* 主色调渐变 */
background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);

/* 改为你喜欢的颜色 */
background: linear-gradient(90deg, #你的颜色1 0%, #你的颜色2 100%);
```

### 修改页面标题

在 `src/App.vue` 中修改：

```vue
<header class="app-header">
  <h1>你的标题</h1>
  <p>你的副标题</p>
</header>
```

在 `index.html` 中修改浏览器标签标题：

```html
<title>你的作品集</title>
```

## 📷 图片处理建议

### 个人照片
- 建议使用专业拍摄的照片
- 背景干净简洁
- 光线充足，清晰度高
- 展现个人风格和气质

### 作品照片
- 使用高质量的作品照片
- 统一的拍摄风格
- 清晰展示设计细节
- 建议每个作品 3-5 张不同角度的照片

### 图片优化工具
- **TinyPNG**：https://tinypng.com/ （压缩图片）
- **Squoosh**：https://squoosh.app/ （在线图片优化）

## 🚀 发布到网络

### 使用 Vercel（推荐，免费）

1. 注册 Vercel 账号：https://vercel.com
2. 导入 GitHub 仓库
3. 自动部署，获得免费域名

### 使用 Netlify（免费）

1. 注册 Netlify 账号：https://netlify.com
2. 拖拽 `dist` 文件夹上传
3. 获得免费域名

## ✏️ 写作技巧

### "关于我"内容建议

```
第一段：简单介绍自己的身份和专业
第二段：描述你的设计理念和风格
第三段：分享你的学习经历或创作感悟
```

例如：
```
你好！我是一名服装设计专业的本科学生，对时尚充满热情。

我热爱通过面料、色彩和剪裁来讲述故事，擅长将传统工艺与现代设计相结合...

在学习过程中，我探索了多种设计风格，从极简主义到复古风格...
```

### 作品描述建议

- 简洁明了，50-100字
- 说明设计理念
- 提及使用的技术或材料
- 突出作品的独特之处

## 🔧 常见问题

### Q: 图片加载不出来怎么办？

A: 如果是 B站等图床的图片，运行 `python download_images.py` 下载到本地。

### Q: 如何添加更多社交媒体链接？

A: 在 `profile.json` 的 `social` 数组中添加：

```json
{
  "name": "平台名称",
  "icon": "emoji图标",
  "url": "链接地址"
}
```

### Q: 如何隐藏社交媒体链接？

A: 在 `profile.json` 中删除或清空 `social` 数组：

```json
"social": []
```

### Q: 作品太多，如何分类？

A: 在 `projects/` 下创建多个 JSON 文件，如：

```
projects/
  ├── fashion-design.json    # 服装设计
  ├── illustration.json      # 插画作品
  └── photography.json       # 摄影作品
```

然后在 `App.vue` 中选择性导入和展示。

### Q: 如何隐藏特别感谢部分？

A: 在 `thanks.json` 中清空数组：

```json
{
  "thanks": []
}
```

或者在 `Home.vue` 中注释掉 `<SpecialThanks />` 组件。

### Q: 感谢对象没有头像怎么办？

A: 可以省略 `avatar` 字段，组件会自动显示默认的用户图标。

## 📚 更多帮助

- 查看 `IMAGE_SOLUTION.md` 了解图片防盗链解决方案
- 查看 `QUICK_FIX.md` 快速修复图片加载问题

## 💡 小贴士

1. 定期备份你的作品和数据
2. 使用高质量图片展示你的作品
3. 保持页面简洁，让作品说话
4. 定期更新作品集内容
5. 可以为不同场合准备不同版本的作品集

---

祝你的作品集大放异彩！✨

