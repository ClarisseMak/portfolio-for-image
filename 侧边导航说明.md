# 📍 侧边导航说明

## ✨ 功能特点

### 1. **固定侧边栏**
- 固定在页面右侧，垂直居中
- 始终可见，不随滚动消失
- 毛玻璃效果，现代美观

### 2. **透明度切换**
- **默认状态**：60% 不透明度，清晰可见
- **悬停状态**：100% 不透明度，完全清晰
- 平滑过渡动画（0.3秒）

### 3. **文字始终显示**
- **默认状态**：完整文字始终显示，方便识别
- **悬停状态**：增强阴影效果，文字变为紫色
- 无需悬停就能看清所有导航项

### 4. **导航功能**
- **About Me**：跳转到个人简介部分
- **各项目分类**：自动根据项目的 category 生成导航
- 点击后平滑滚动到对应位置

## 🎯 导航项

侧边导航会自动生成以下导航项：

```
● About Me
● School Projects
● Company Projects  
● Personal Projects
... (根据你的项目分类自动生成)
```

## 🎨 视觉效果

### 默认状态（未悬停）
```
    ┌────────────────────┐
    │  ●  About Me       │  60% 透明度
    │  ●  School Proj... │  文字完整显示
    │  ●  Company Proj..│  较轻阴影
    └────────────────────┘
```

### 悬停状态
```
    ┌────────────────────┐
    │  ●  About Me       │  100% 透明度
    │  ●  School Proj... │  文字变紫色
    │  ●  Company Proj..│  增强阴影
    └────────────────────┘
```

## 🖱️ 交互细节

### 圆点动画
- 悬停在导航项上时，圆点会放大 1.3 倍
- 圆点会发出淡紫色光晕
- 文字颜色变为紫色 (#667eea)

### 滚动行为
- 点击导航项后平滑滚动
- 自动计算偏移量（避免被顶部遮挡）
- 滚动到目标位置上方 80px 处

## 📱 响应式设计

### 桌面端（>1024px）
- ✅ 显示侧边导航
- 位置：右侧 2rem
- 大屏幕（>1400px）：右侧 3rem

### 平板/移动端（≤1024px）
- ❌ 隐藏侧边导航
- 原因：屏幕空间有限，避免遮挡内容

## 🛠️ 技术实现

### 自动生成导航项
导航项会根据 `example.json` 中的项目分类自动生成：

```json
{
  "projects": [
    {
      "category": "School Projects",  // 自动生成导航项
      ...
    }
  ]
}
```

### ID 生成规则
分类名称会转换为 ID：
- `"School Projects"` → `"school-projects"`
- `"Company Projects"` → `"company-projects"`
- 空格转为连字符，全部小写

## 🎨 自定义样式

### 修改位置
在 `SideNav.vue` 中：

```css
.side-nav {
  right: 2rem;  /* 修改距离右侧的距离 */
  top: 50%;     /* 修改垂直位置 */
}
```

### 修改透明度
```css
.side-nav {
  opacity: 0.6;  /* 默认透明度（60%），可改为 0.5-0.8 */
}

.side-nav.is-hovered {
  opacity: 1;    /* 悬停时透明度（100%） */
}
```

### 修改圆点颜色
```css
.nav-dot {
  background: #667eea;  /* 改为你喜欢的颜色 */
}
```

### 修改背景样式
```css
.side-nav {
  background: rgba(255, 255, 255, 0.95);  /* 白色背景 */
  backdrop-filter: blur(10px);             /* 毛玻璃效果 */
  border-radius: 30px;                     /* 圆角 */
}
```

## 📝 添加新的导航项

侧边导航会自动根据项目分类生成，无需手动添加。只需在 `example.json` 中添加新的项目：

```json
{
  "id": 4,
  "category": "新分类名称",  // 会自动出现在侧边导航
  ...
}
```

## 🔧 常见问题

### Q1: 为什么导航项不显示？
**A:** 检查以下几点：
1. 确保浏览器宽度 > 1024px
2. 确保项目有 `category` 字段
3. 刷新页面

### Q2: 如何修改悬停延迟？
**A:** 在 `SideNav.vue` 中修改 transition 时间：
```css
.side-nav {
  transition: all 0.3s ease;  /* 改为你想要的时间 */
}
```

### Q3: 能否在左侧显示？
**A:** 可以，修改 `right` 为 `left`：
```css
.side-nav {
  left: 2rem;   /* 改为左侧 */
  /* right: 2rem; 删除这行 */
}
```

### Q4: 如何禁用平滑滚动？
**A:** 在 `App.vue` 中移除：
```css
html {
  /* scroll-behavior: smooth; 注释或删除这行 */
}
```

### Q5: 导航项太多怎么办？
**A:** 导航会自动适应内容数量。如果太多，可以：
1. 减小 gap 间距
2. 减小字体大小
3. 减小 padding

## 💡 使用技巧

### 1. 快速定位
使用侧边导航可以快速在长页面中定位到目标内容

### 2. 视觉引导
默认的半透明状态既不抢眼，又能引导用户发现导航

### 3. 分类管理
通过合理的项目分类，让导航更清晰有序

### 4. 适配内容
如果只有 2-3 个分类，导航会很简洁
如果有很多分类，导航会完整展示所有内容

## 🎯 最佳实践

1. **保持分类简洁**：建议 3-6 个分类最佳
2. **统一命名风格**：使用相同的命名模式
3. **逻辑分组**：按时间、类型或主题分类
4. **避免过长名称**：建议不超过 20 个字符

## 📚 相关文件

- `src/components/SideNav.vue` - 侧边导航组件
- `src/views/Home.vue` - 首页（使用导航）
- `src/App.vue` - 全局样式（平滑滚动）

---

享受更流畅的浏览体验！✨

